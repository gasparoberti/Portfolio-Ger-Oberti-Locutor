@model PortfolioCore.Models.Tip

@{
    ViewData["Title"] = "DetalleTip";
}


<h1 class="display-4 text-center">@Model.titulo</h1>

<div class="row text-muted text-justify mb-5">
    <h5>@Model.descripcion</h5>
</div>

<div class="row">
    @if (Model.id % 2 == 0)
    {
        <p class="text-justify">
            <img class="ml-4" align="right" src="@("~/image/" + Model.imagen)" asp-append-version="true" alt="Tip image" style="object-fit: cover; width: 40vw" />
            @Model.contenido
        </p>
    }
    else
    {
        <p class="text-justify">
            <img class="mr-4" align="left" src="@("~/image/" + Model.imagen)" asp-append-version="true" alt="Tip image" style="object-fit: cover; width: 40vw" />
            @Model.contenido
        </p>
    }
</div>

@*@if (Model.video != null)
{
    <div class="row justify-content-center m-5">
        <iframe width="492" height="277" class="embed-responsive-item" src="https://www.youtube.com/embed/zpOULjyy-n8" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    </div>
}*@

<div class="row justify-content-center">
    <a casp-action="Tips" class="btn btn-light m-3">Todos los tips</a>
</div>

